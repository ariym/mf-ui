export default () => (
  <div className="flex items-center justify-center h-screen" style={{marginTop:-100}}>
    <p className="text-center text-4xl">
      Error 404. URL does not exist.
    </p>
  </div>
);
